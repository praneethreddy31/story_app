import express from 'express';

const router = express.Router();

// Placeholder routes for AI
router.get('/', (req, res) => {
  res.json({ success: true, message: 'AI routes will be implemented here' });
});

export default router;
